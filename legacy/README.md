Legacy files kept for reference only.

These assets are not part of the active Kreativ Font product path. They were moved out of the active `js/` folder so the theme surface is easier to audit while preserving old code if it is ever needed for comparison.
